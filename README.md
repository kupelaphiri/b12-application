# B12 Application — Kupela Phiri

This repository contains my application to B12 as a Full Stack Engineer.

The `apply.py` script submits my application to B12's submission endpoint, signed with HMAC-SHA256, and is executed automatically via GitHub Actions on every push to `main`.
